<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruos.textactions"
    android:versionCode="10000"
    android:versionName="1.0">

    <application
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- «Найти» — appears in the text-selection toolbar of any app. -->
        <activity
            android:name=".SearchTextActivity"
            android:label="@string/text_search"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- «Перевести» — opens Yandex Translate with the selection. -->
        <activity
            android:name=".TranslateTextActivity"
            android:label="@string/text_translate"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
