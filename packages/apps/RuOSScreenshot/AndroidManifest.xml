<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruos.screenshot"
    android:versionCode="10000"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MarkupTheme"
        android:supportsRtl="true"
        android:hardwareAccelerated="true">

        <!-- Launched by SystemUI when the screenshot thumbnail is tapped (image URI). -->
        <activity
            android:name=".MarkupActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:screenOrientation="portrait" />

        <!-- Scrolling-screenshot stitcher: fed an ordered list of slice URIs, either via the
             custom STITCH action or by multi-sharing images from the gallery. -->
        <activity
            android:name=".scroll.StitchActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/stitch_label"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="com.ruos.screenshot.STITCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- Document scanner: perspective-correct + scan filter + PNG/PDF export. -->
        <activity
            android:name=".scanner.DocScanActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="com.ruos.screenshot.SCAN_DOCUMENT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>
