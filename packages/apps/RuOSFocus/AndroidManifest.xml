<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruos.focus"
    android:versionCode="10000"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/FocusTheme"
        android:supportsRtl="true"
        android:hardwareAccelerated="true">

        <!-- Focus home — also a launcher icon for discoverability. -->
        <activity
            android:name=".ui.FocusListActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.FocusEditActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.AppPickerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Time-based activation: boundary alarms + boot/time re-evaluation. -->
        <receiver
            android:name=".schedule.FocusAlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ruos.focus.EVALUATE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>
    
        <provider
            android:name=".backup.BackupProvider"
            android:authorities="com.ruos.focus.backup"
            android:exported="true"
            android:permission="com.ruos.permission.BACKUP" />
    </application>
</manifest>
